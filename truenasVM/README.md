# TrueNAS VM Factory

Reusable OpenTofu provisioning and Ansible configuration for Ubuntu VMs on TrueNAS SCALE.

Run `make new-vm` to choose a name and a suggested `10.0.203.x` address, then run `make deploy VM=<name>`.
