---
- name: Run pcData backup now
  hosts: all
  become: true
  gather_facts: false
  tasks:
    - name: Start and wait for pcData backup
      ansible.builtin.systemd_service:
        name: pcdata-backup.service
        state: started
